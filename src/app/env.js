(function(window) {
  window._env_ = window._env_ || {};
  
  // API URL configuration
  window._env_.apiUrl = window._env_.apiUrl || "/.netlify/functions/api";
  
  // Any other environment variables needed for the app
})(this); 